[build]
  publish = "public"
  command = "node scripts/build-env.js"

[build.environment]
  NODE_VERSION = "18"

# SPA: alle paden naar index.html; /.netlify/functions/* wordt door Netlify apart afgehandeld
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
